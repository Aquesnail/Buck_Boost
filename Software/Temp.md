目前按键和旋钮编码器已经测试完毕，我想先把UI和交互写了，留下对应的句柄钩子，要求如下:
分成两个界面，可以通过按键来切换
一个界面是调整输出电压和电流，只有两个功能，恒压输出和限流
显示两排大字参数（目前大字字体还没准备好，你先算好位置和大小，然后随便放两行字凑合下，后面我会更换字体）
当进入调整电压模式时，此时不显示当前电压，显示我们的设定电压
电感进入调整限流模式时，此时不显示当前电压，显示我们的设定电流
通过编码器和按钮来调整参数，调整的逻辑你来定
一个界面用于调整电压控制环路的参数
这个控制环路和参数的数量还没有确定，所以你最好写一个方便我增删参数的代码结构，先简单留下Pi的参数吧，这个界面要显示输入输出电压，输入输出电流，温度，电感电流，布局你来决定
读取编码器是直接读取TIM1寄存器，但是我还没有给它包装函数解耦，这一步也要你也帮我做一下吧
要求有点多，你可以先把我的要求分成几个层级，我们慢慢完善
